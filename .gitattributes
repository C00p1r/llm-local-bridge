# 僅對「CRLF 會導致執行失敗」的檔案強制 LF。
# 其餘檔案（Python、Markdown、JS 等）維持倉庫原有行尾，避免產生大量無謂的 diff 雜訊。

# Shell 腳本：CRLF 會使 shebang 失效，並觸發 'set: pipefail: invalid option name'
*.sh text eol=lf

# Dockerfile：CRLF 會破壞 RUN 行接續的反斜線
Dockerfile text eol=lf
